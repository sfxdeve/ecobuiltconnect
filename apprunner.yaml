version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - corepack enable
      - pnpm install --frozen-lockfile
      - pnpm build

run:
  command: node .output/server/index.mjs
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
